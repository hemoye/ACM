#include <stdio.h>

int main(void)
{
    int muban, num_s, num_c;
    int i, j, a[1000];

    scanf("%d %d %d", &muban, &num_s, &num_c);
    for (i = 0; i < num_c; i++)
        scanf("%d", &a[i]);

    return 0;
}
