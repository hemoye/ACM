#include <stdio.h>

int main(void)
{
    int number, m, n, i, j;
    int sum = 0, num1 = 0, num2 = 0, a[1000000] = {0};

    scanf("%d", &number);
    for (i = 0; i < number; i++)
    {
        scanf("%d%d", &m, &n);
        for (j = m; j <= n; j++)
            a[j] = 1;
    }
    for (i = 0; i < len; i++)
        num++;

    return 0;
}
