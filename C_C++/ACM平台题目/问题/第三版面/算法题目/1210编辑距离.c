#include <stdio.h>
#include <string.h>

int main(void)
{
    char str1[1000], str2[1000];
    int i, j;
    int step = 0;

    gets(str1);
    gets(str2);
    if (strcmp (str1, str2) < 0)
    {
        step = strlen(str2);
        for (i = 0; i < strlen(str1); i++)
            for (j = 0; j < )
    }

    return 0;
}
