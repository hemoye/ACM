	标准的汉诺塔上有n个大小各异的盘子.给定一个初始局面(a),求他到给定目标局面(b)至少需要多少步.移动规则如下:一次只能移动一个盘仔细;在移动一个盘子之前,必须把压在上面的其他盘子先移走;编号大的盘子不得压在编号小的盘子上.
　　　
　　　(a)

　　　(b)

	输入包含不超过100组数据.每组数据的第一行为正整数n(1≤n≤60);第二行包含n个1~3的整数,即初始化局面中每个盘子所在的柱子编号;第三行和第二行格式相同,为目标局面.输入结束的标志为n=0.

	对于每组数据,输出最小步数.