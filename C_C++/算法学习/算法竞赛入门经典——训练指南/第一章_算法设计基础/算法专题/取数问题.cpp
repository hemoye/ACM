#include <cstdio>
#define  MAXN 40

int main( int agrc, char *agrv[] )
{
    char str[MAXN];
    int n;
    while ( ~scanf("%s", str ) ){
        scanf("%d", &n );
        int len = strlen( str );
        int nen = len - n;
        for ( int i = 0; i < nen; i++ ){

            for ( int j = 0; j < i; j++ )
        }
    }

    return 0;
}
