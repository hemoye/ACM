#include <cstdio>
#include <cstring>
#define  MAXN 1004

int main( int agrc, char *agrv[] )
{
    int N;
    char str[MAXN];
    scanf("%d%*c", &N );
    while ( N-- ){
        gets( str );
        int len = strlen( str );
        int n(0);
        for ( int i = 0; i < len - 1; i++ ){
            if ( str[i] == str[i+1] ){
                n++;
            }
            else {
                if ( n >)
            }
        }
    }

    return 0;
}
